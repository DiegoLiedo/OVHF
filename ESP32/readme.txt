This folder contains the codes used to make the ESP32's from OVHF work as a wireless multichannel MIDI transmitter and receiver.
These code is written in arduino using arduino libraries (included in libraries/)
